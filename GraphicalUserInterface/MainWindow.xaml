<Window x:Class="TP.ConcurrentProgramming.PresentationView.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:vm="clr-namespace:TP.ConcurrentProgramming.Presentation.ViewModel;assembly=TP.ConcurrentProgramming.PresentationViewModel"
        xmlns:presentationView="clr-namespace:TP.ConcurrentProgramming.PresentationView"
        mc:Ignorable="d"
        Title="Presentation layer targeting reactive behaviour" Height="auto" Width="auto">
    <Window.DataContext>
        <vm:MainWindowViewModel />
    </Window.DataContext>
    <Window.Resources>
        <presentationView:ColorConverter x:Key="colorConverter"></presentationView:ColorConverter>
    </Window.Resources>
    <StackPanel>
        <Border BorderThickness="4" BorderBrush="Green" Height="420" Width="400">
            <ItemsControl ItemsSource="{Binding Balls}">
                <ItemsControl.ItemsPanel>
                    <ItemsPanelTemplate>
                        <Canvas />
                    </ItemsPanelTemplate>
                </ItemsControl.ItemsPanel>
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <Ellipse Name="Ball" Width= "{Binding Diameter}" Height="{Binding Diameter}" Fill="{Binding Mass, Converter = '{StaticResource colorConverter}' }"/>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
                <ItemsControl.ItemContainerStyle>
                    <Style TargetType="ContentPresenter">
                        <Setter Property="Canvas.Top" Value="{Binding Top}" />
                        <Setter Property="Canvas.Left" Value="{Binding Left}" />
                    </Style>
                </ItemsControl.ItemContainerStyle>
            </ItemsControl>
        </Border>
        <StackPanel Orientation="Horizontal" Width="100" HorizontalAlignment="Center">
            <TextBox Width="70" x:Name="BallsQuantity"></TextBox>
            <Button Width="30" Click="Button_Click">Add</Button>
        </StackPanel>
    </StackPanel>
</Window>